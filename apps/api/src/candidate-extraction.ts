import { CandidateStructuredData } from '@rr/shared';
const empty=():CandidateStructuredData=>({candidate_name:null,email:null,phone:null,location:null,summary:null,skills:[],education:[],experience:[],projects:[],certifications:[],additional_information:[]});
export function extractCandidateData(text:string):CandidateStructuredData{const out=empty();const lines=text.split(/\r?\n/).map(x=>x.trim()).filter(Boolean);out.candidate_name=lines.find(x=>/^[A-Z][A-Za-z.'-]+(?:\s+[A-Z][A-Za-z.'-]+){1,3}$/.test(x))||null;out.email=text.match(/[\w.+-]+@[\w.-]+\.[A-Za-z]{2,}/)?.[0]||null;out.phone=text.match(/(?:\+?\d[\d ()-]{7,}\d)/)?.[0]||null;out.location=lines.find(x=>/\b(ON|Ontario|Toronto|Waterloo|Canada|CA|NY|USA)\b/i.test(x))||null;const skillIndex=lines.findIndex(x=>/^skills?\b/i.test(x));const skillLine=skillIndex>=0?lines[skillIndex].replace(/^skills?\s*[:\-]?\s*/i,'')||lines[skillIndex+1]:undefined;if(skillLine)out.skills=skillLine.split(/,|\s{2,}/).map(x=>x.trim()).filter(Boolean);const summaryIndex=lines.findIndex(x=>/^summary\b/i.test(x));if(summaryIndex>=0)out.summary=lines[summaryIndex+1]||null;for(const line of lines){if(/^[-•]/.test(line))out.additional_information.push(line.replace(/^[-•]\s*/,''));}return CandidateStructuredData.parse(out);}
